const menu = () => {
    const element = document.createElement('div');
    const card = document.createElement('div');

    element.setAttribute('class', 'menu-section');
    card.setAttribute('class', 'menu-grid');

    return element;
}

export default menu;